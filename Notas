JQUERY
FUNCION PARA INICIALIZAR
$(document).ready(funcion a ejecutar);

SELECTORES
$(selector).hide();
el selector es igual que en css ejemplo('h1','.clase','#id','ul li')

EFECTOS
$(selector).efecto(velocidad(en milisegundos),callback);

show hide toggle(alterna show y hide):
Ejemplo:
$('.caja').hide(3000,funcion);
$('.caja').show(3000,funcion);
$('.caja').toggle(3000,funcion);

fadeIn, fadeOut, fadeToggle
$('.caja').fadeIn(3000,funcion);
$('.caja').fadeOut(3000,funcion);
$('.caja').fadeToggle(3000,funcion);

slidedown, slideUp, slideToggle
$('.caja').slideDown(3000,funcion);
$('.caja').slideUp(3000,funcion);
$('.caja').slideToggle(3000,funcion);

ENCADENAMIENTO
$('#tercera').parent().css();

APLICAR CSS CON JQUERY
Forma como paremetro
$('#tercera').parent().css('background','#fff');
Forma como objeto
$('#tercera').parent().css({
    background:'#fff'
});

OBTENER ELEMENTOS DEL DOM
Obtener elemento padre
    $('#tercera').parent();

Obtener todos los elementos padres
    $('#tercera').parents

OBTENER LOS ELEMENTOS HIJOS DIRECTOS(todos)
    $('#padre').children();

OBTENER ELEMENTOS HIJOS DIFERENCIANDO CON PARAMETRO DIRECTOS(class,id, etc)
    $('#padre').children('#hijo');

OBTENER ELEMENTOS HIJOS PERO BUSCANDO EN VARIOS NIVELES
    $('#contenedor').find('div.caja');

OBTENER ELEMENTOS HERMANOS(pero no si mismo)
    $('#tercera').siblings();

OBTENER ELEMENTOS SIGUIENTE Y ANTERIOR(en el mismo nivel)
    $('#tercera').prev();
    $('#tercera').next();

OBETENER TODOS LOS ELEMENTOS ANTERIORES Y TODOS LOS ELEMENTOS POSTERIORES HERMANOS
    $('#tercera').prevAll();
    $('#tercera').nextAll();

//SELECCIONA LA PRIMERA CAJA
    $('.caja').first().css({background:'#000'});
//SELECCIONA LA ULTIMA CAJA
    $('.caja').last().css({background:'#000'});
//SELECCIONA CON EL INDEX
    $('.caja').eq(2).css({background:'#000'});
//SELECCIONAR CAJA DEPENDIENDO DE ALGUN ATRIBUTO
    $('.caja').filter('#segunda').css({background:'#000'});
//SELECCIONAR FILTRANDO POR NEGACION
    $('.caja').not('#segunda').css({background:'#ff2'});

EVENTOS!!!!!!

 //POR MEDIO DE FUNCION ANONIMA
   boton.click(function(){
       alert('Hola');
   });
    //POR MEDIO DE FUNCION CREADA
    boton.click(saludo);
//METODO MAS UTILIZADO EN LA ACTUALIZADAD 
    LLAMANDO FUNCION
    boton.on('click',funcion);
    UTILIZANDO CALLBACK
    boton.on('mouseleave',function(){
        $('body').css({background:'#000'});
    });
//ELIMINAR EVENTOS
    boton.off('click');
//PREVENIR COMPORTAMIENTOS (EJEMPLO CON ENLACES)
    $('a').on('click',function(e){
        e.preventDefault();
    });
//CAMBIAR CLASES CON JQUERY
$('.caja').toggleClass(color);

//PALABRA RESERVADA THIS
Ejemplo:
$('.caja').on('click',function(){
    $(this).toggleClass('color');
});

ANIMACIONES JQUERY!!!!!!!!!!!!
Estructura:
$(selector).animate({parametros},velocidad,callback);
Ejemplo solo con parametros
$('#caja').animate({
    width:'300px', //Propiedad CSS dentro de un objeto jquery
    opacity:'0.2'
});
Ejemplo con velocidad y callback:
$('#caja').animate({
    width:'300px', //Propiedad CSS dentro de un objeto jquery
    opacity:'0.2'
},2000,function(){
    alert('fin de la animacion');
});
Truquito!! ir sumando valores:
$('#caja').animate({
    width:'+=300px'
},1000);
En Jquery no es posible animar colores, esto se puede hacer mezclando css3 Ejemplo:
//Agregar keyFrame al estilo
@keyframe colores{
    from{background:#102938;}
    to{background:#000}
}
//Poner animacion en una clase
.animacion{
    animation:colores 3s ease;
}

//Por ultimo agregar la clase en Jquery
$('#boton').on('click',function(){
    $('#caja').addClass('animacion');
});

PARAR ANIMACIONES
$('#parar').on('click',function(){
        //.stop(limpiarAnimaciones,Saltar al final);
        $('.caja').stop(true,false);
    });

CALLBACK
Funcion que se realiza despues de terminada una accion

MANIPULANDO EL DOM
Tomar y modificar texto de una marca html
$('#titulo').text(); toma el valor del texto dentro de la marca
$('#titulo').text('Texto modificado');modificar el valor del texto dentro de la marca

Tomar e insertar codigo html
$('#titulo').html('<ul>Subrayado</ul>'); Inserta dentro del elemento codigo html

Tomar y cambiar valor (value) de un elemento
$('#nombre').val(); //Toma el valor
$('#nombre').val('Enviar') //Setea el valor

Tomar y modificar atributos:
$('#enlace').attr('href') //Toma el valor del atributo
$('#enlace').attr('href','http://www.yahoo.com') modifica el valor del atributo

Modificar multiples atributos(por medio de un objeto)
$('#enlace').attr({
    'class':'azul';
    'target':'_blank'
});

AGREGANDO ELEMENTOS HTML AL DOM CON JQUERY
para crear un elemento en este caso un div lo hacemos como un selector, en el ejemplo le ponemos tambien el atributo class y un texto:
    var caja = $('<div></div>').attr('class','caja').text('Hola');
paso siguiente lo metemos en un contenedor (o body):
    $('#contenedor').append(caja);
Para colocar el elemento al inicio:
    $('#contenedor').prepend(caja);
Para agregar antes del contenedor(no adentro):
    $('#contenedor').before(caja);
Para agregar despues del contenedor(no adentro):
    $('#contenedor').after(caja);

BORRAR ELEMENTOS HTML DEL DOM CON JQUERY
Para borrar el mas utilizado es el remove, Ejemplos:
    $('#contenedor').remove();
    $('#contenedor').children().remove();
Para borrar los elementos contenidos de un elementos HTML se usa empty, Ejemplo:
    $('#contenedor').empty();

TRABAJANDO CON CSS DESDE JQUERY
Agregar class:
$('#boton').addClass('naranja');
Sacar class:
$('#boton').removeClass('naranja');
Alternar Class:
$('#boton').toggleClass('naranja');
Modificar Css por medio de un objeto:
$('#boton').css({
    'width':'100px',
    'height':'100px' //El ultimo no lleva coma;
});

MEDIDAS JQUERY
Obtener el width:
$('#titulo').width();
Obtener el width con el padding incluido:
$('#titulo').innerWidth();
Obtemer el width con el padding + border incluido:
$('#titulo').outerWidth();
Width con padding + border + margin:
    $('#titulo').outerWidth(true);

Para el height es lo mismo pero usando .height():
titulo.height();
titulo.innerHeight();
titulo.outerHeight();
titulo.outerHeight(true);

PONER UNA FUNCION A CADA UNO DE LOS ELEMENTOS
$('.menu a').each(function(index,elemento){

});

CALCULAR POSICION EN PANTALLA DE ELEMENTO JQUERY
$('#acerca-de').offset().top;

COMO IR A UN ELEMENTO POR MEDIO DE UNA ANIMACION Y EL SCROLL
 var acercaDe = $('#acerca-de').offset().top,
        menu = $('#platillos').offset().top,    
        galeria = $('#galeria').offset().top,
        ubicacion = $('#ubicacion').offset().top;

    $('#btn-acerca-de').on('click',function(){
        $('html, body').animate({
            scrollTop:acercaDe -100
        },500);
    });
